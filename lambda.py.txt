
import random

def parseInt(value):
    try:
        return int(value)
    except ValueError:
        return 100

def lambda_handler(event, context):
    print(event)
    upperLimitDict = event['request']['intent']

    number = random.randint(0, 100)
    response = {
        'version': '1.0',
        'response': {
            'outputSpeech': {
                'type': 'PlainText',
                'text': 'The temperature of X D K Sensor Bosch is ' + str(number),
            }
        }
    }

    return response